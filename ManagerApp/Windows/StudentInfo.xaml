<Window x:Class="ManagerApp.Windows.StudentInfo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ManagerApp.Windows"
        mc:Ignorable="d"
        Title="StudentInfo" Height="450" Width="800">
    <Grid>
        

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="80"/>
            </Grid.RowDefinitions>

            <!-- First Name -->
            <TextBlock Text="First name" FontSize="18" VerticalAlignment="Center"/>
        <TextBox Grid.Column="1" Margin="0,5" Height="40" FontSize="16" Text="{Binding StudentFirstName}"/>

            <!-- Last Name -->
            <TextBlock Grid.Row="1" Text="Last name" FontSize="18" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,5" Height="40" FontSize="16" Text="{Binding StudentLastName}"/>

            <!-- City -->
            <TextBlock Grid.Row="2" Text="City" FontSize="18" VerticalAlignment="Center"/>
            <!--<ComboBox Grid.Row="2" Grid.Column="1" Height="40" Margin="0,5" FontSize="16">
                <ComboBoxItem Content="Select"/>
                <ComboBoxItem Content="Tel Aviv"/>
                <ComboBoxItem Content="Jerusalem"/>
                <ComboBoxItem Content="Haifa"/>
            </ComboBox>-->
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,5" Height="40" FontSize="16" Text="{Binding CityId}"/>

        <!-- Telephone -->
        <TextBlock Grid.Row="3" Text="Telephone" FontSize="18" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Margin="0,5" Height="40" FontSize="16" Text="{Binding StudentTelephone}"/>

            <!-- Address -->
            <TextBlock Grid.Row="4" Text="Address" FontSize="18" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Grid.Column="1" Margin="0,5" Height="40" FontSize="16" Text="{Binding StudentAddress}"/>
        <!-- Buttons -->
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="0,20,0,0">
            <Button Content="Close" Width="150" Height="50" FontSize="18" Margin="20,0" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
